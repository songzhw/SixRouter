<?xml version="1.0" encoding="utf-8"?>
<manifest package="ca.six.router.demo.app"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".MainApp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.SixRouter">

        <activity android:name=".biz.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".biz.DeepLinkActivity" />
        <activity android:name=".biz.LoginActivity" />
        <activity android:name=".biz.TouristActivity" />
    </application>

</manifest>